app_deployment:
  name: app-deployment
  replicas: 2
  namespace: default
  containers:
    name: room-container
    image: nvb20/chat-app:testings
    containerPort: 5000
  

app_service:
  name: app-service
  port: 80
  targetPort: 5000
